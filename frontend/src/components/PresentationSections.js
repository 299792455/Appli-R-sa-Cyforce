import React from 'react';
import '../styles/PresentationSection.scss';


function PresentationSection() {
  return (
    <section className="presentation">
      <h2>Pourquoi utiliser notre service de réservation ? (A voir si on garde)</h2>
      <p>Grâce à notre service, visualisez et planifiez facilement vos créneaux et suivez l'évolution de vos chevaux à tout moment.</p>
    </section>
  );
}

export default PresentationSection;
